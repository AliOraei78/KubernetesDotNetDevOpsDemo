apiVersion: v1
kind: Service
metadata:
  name: devopsdemo-service
  labels:
    app: devopsdemo
spec:
  type: NodePort
  selector:
    app: devopsdemo
  ports:
  - port: 8080
    targetPort: 8080
    nodePort: 30080